
<% if flash.any? %>
    <% flash.each do |type, message| %>
      <div data-alert class="alert-box radius <%= type %> small-8 columns">
        <%= message %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>
<% end %>
